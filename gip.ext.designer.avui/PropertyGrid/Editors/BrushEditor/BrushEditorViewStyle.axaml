<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Designer="clr-namespace:gip.ext.designer.avui"
                    xmlns:Controls="clr-namespace:gip.ext.designer.avui.Controls"
                    xmlns:BrushEditor="clr-namespace:gip.ext.designer.avui.PropertyGrid.Editors.BrushEditor"
                    xmlns:Converters="clr-namespace:gip.ext.designer.avui.Converters">

  <ControlTheme x:Key="{x:Type BrushEditor:BrushEditorView}" TargetType="BrushEditor:BrushEditorView">
    <Setter Property="Width" Value="395" />
    <Setter Property="Template">
      <ControlTemplate TargetType="BrushEditor:BrushEditorView">
        <TabControl x:Name="PART_TabControl"
                    Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                    SelectedIndex="{Binding CurrentKind, Converter={x:Static Converters:IntFromEnumConverter.Instance}}">
          <TabItem Header="None">
            <Border Background="White"
                    BorderThickness="1"
                    BorderBrush="Black"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
              <Line StartPoint="0,40"
                    EndPoint="70,0"
                    Stroke="Red"
                    StrokeThickness="1" />
            </Border>
          </TabItem>
          <TabItem Header="Solid">
            <BrushEditor:SolidBrushEditor Color="{Binding Color}" />
          </TabItem>
          <TabItem Header="Linear">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
              <StackPanel Grid.Row="0"
                          Orientation="Horizontal"
                          Margin="5">
                <TextBlock Text="Angle"
                           VerticalAlignment="Center" />
                <Controls:NumericUpDown Value="{Binding GradientAngle}"
                                        Minimum="-360"
                                        Maximum="360"
                                        Margin="5 0 0 0"
                                        Width="50" />
                <Button Content="H"
                        Command="{Designer:Call MakeGradientHorizontal}"
                        Margin="5 0 0 0"
                        Width="30" />
                <Button Content="V"
                        Command="{Designer:Call MakeGradientVertical}"
                        Margin="5 0 0 0"
                        Width="30" />
              </StackPanel>
              <BrushEditor:GradientBrushEditor Grid.Row="1" Brush="{Binding LinearBrush}" />
            </Grid>
          </TabItem>
          <TabItem Header="Radial">
            <BrushEditor:GradientBrushEditor Brush="{Binding RadialBrush}" />
          </TabItem>
          <TabItem Header="Brush List">
            <ListBox ItemsSource="{Binding AvailableBrushes}"
                     SelectedValue="{Binding Color}">
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal">
                    <Border Background="{Binding Brush}"
                            Width="30"
                            Height="15"
                            Margin="2"
                            BorderThickness="1"
                            BorderBrush="Black" />
                    <TextBlock Text="{Binding Name}"
                               VerticalAlignment="Center" />
                  </StackPanel>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
          </TabItem>
        </TabControl>
      </ControlTemplate>
    </Setter>
  </ControlTheme>

</ResourceDictionary>
