<Window x:Class="gip.tool.publish.ClearPass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gip.tool.publish"
        mc:Ignorable="d"
        Title="Replace a password in the *.config files" Height="140" Width="450" Background="#FF333333" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" Foreground="White" Background="Transparent" Margin="5,0,5,0"
                   Text="Enter the word that will be replaced with 'password' in the *.config files. If you won't replace any word leave field blank!" 
                   VerticalAlignment="Center" TextWrapping="Wrap"/>
        <TextBox Background="Black" Foreground="White" Height="25"  Grid.ColumnSpan="2" Grid.Row="1" x:Name="TBpassToClear" VerticalAlignment="Center" Margin="5,0,5,0" />
        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="OK" Click="Button_Click" Margin="5,0,5,0" Height="25" />
    </Grid>
</Window>
