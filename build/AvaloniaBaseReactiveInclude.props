<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup>
    <!-- Intentionally left empty.
         ReactiveUI bridge package should be referenced only by projects that need it,
         to avoid bringing incompatible compile assets into all Avalonia fork projects. -->
  </ItemGroup>
</Project> 