<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:le="clr-namespace:gip.core.layoutengine"
    xmlns:local="clr-namespace:gip.core.layoutengine.timeline">

    <Style x:Key="TimelineItemStyleAero" TargetType="{x:Type local:TimelineItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TimelineItem}">
                    <ControlTemplate.Resources>
                        <ResourceDictionary>
                            <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}" >
                                <Setter Property="Background" Value="Beige"></Setter>
                                <Setter Property="BorderBrush" Value="DarkRed" />
                            </Style>
                        </ResourceDictionary>
                    </ControlTemplate.Resources>
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>