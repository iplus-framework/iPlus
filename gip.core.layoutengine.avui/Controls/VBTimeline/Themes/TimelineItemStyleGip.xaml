<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:le="clr-namespace:gip.core.layoutengine.avui"
    xmlns:local="clr-namespace:gip.core.layoutengine.avui.timeline">

    <Style x:Key="TimelineItemStyleGip" TargetType="{x:Type local:TimelineItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TimelineItem}">
                    <ControlTemplate.Resources>
                        <ResourceDictionary>
                            <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}" >
                                <Setter Property="Background" Value="#070707"></Setter>
                                <Setter Property="BorderBrush" Value="DarkRed" />
                            </Style>
                        </ResourceDictionary>
                    </ControlTemplate.Resources>
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>